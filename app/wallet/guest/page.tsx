import { GuestWalletTopup } from "@/components/guest-wallet-topup";

export const metadata = {
  title: "Guest Wallet Top-Up",
  description: "Add funds to your wallet instantly",
};

export default function GuestWalletPage() {
  return <GuestWalletTopup />;
}
